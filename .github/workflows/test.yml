name: Test and Lint

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '7.4'
        extensions: mbstring, intl
        coverage: none
        tools: composer, cs2pr

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: 'npm'

    - name: Install dependencies
      run: composer update --prefer-dist --no-progress --no-interaction

    - name: Install Node dependencies
      run: npm install

    - name: Build block
      run: npm run build

    - name: Run JS Lint
      run: npm run lint:js

    - name: Run CSS Lint
      run: npm run lint:css

    - name: Run PHPCS
      run: vendor/bin/phpcs -q --report=checkstyle | cs2pr

    - name: Setup WordPress Test Suite
      run: |
        curl -O https://raw.githubusercontent.com/wp-cli/wp-cli/master/templates/install-wp-tests.sh
        chmod +x install-wp-tests.sh
        bash install-wp-tests.sh wordpress_test root '' 127.0.0.1 latest

    - name: Run PHPUnit
      run: vendor/bin/phpunit
